{
  "name": "mapadillo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "npm run build && npm run migrate:local --prefix worker && npm run dev --prefix worker",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm test --prefix worker",
    "test:watch": "npm run test:watch --prefix worker",
    "test:ui": "vitest run",
    "test:all": "vitest run && npm test --prefix worker"
  },
  "dependencies": {
    "@awesome.me/kit-781a3c6be3": "^1.0.4",
    "@better-auth/passkey": "^1.6.2",
    "@watergis/maplibre-gl-export": "^4.1.1",
    "@web.awesome.me/webawesome-pro": "^3.5.0",
    "better-auth": "^1.6.2",
    "jspdf": "^4.2.1",
    "lit": "^3.3.2",
    "maplibre-gl": "^5.23.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "~4.1.4",
    "happy-dom": "^20.8.8",
    "typescript": "^6.0.2",
    "vite": "^8.0.8",
    "vitest": "~4.1.4"
  }
}
